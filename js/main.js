const avoidCrawlerObu = 'foo',
  domain = 'bar.com',
  element = document.getElementById('email');

element.innerHTML = user + '@' + domain;
//OR
//'<a href="mailto:' + user + '@' + domain + '">Email</a>'
